int main () {
    int a = 1 -- 2;
    int b = 5----12*11/6+-3;
    int c = 123++++55-+-+-9961;
    int d = -2, result = 2, useless = 13;
    if(d)if(d)if(d);else;else
    if ((d * 1 / 2) < 0 || (a - b) != 0 && (c + 3) % 2 != 0) {
        while(1){}if(1){}
        putint(result);
    }
    if ((d % 2 + 67) < 0 || (a - b) != 0 && (c + 2) % 2 != 0) {
        while(1);
        if(0);
        while(1){
            putint(-1--0x80000000);
        }
        result = 4;
        putint(result);
    }
    return 0;
}
